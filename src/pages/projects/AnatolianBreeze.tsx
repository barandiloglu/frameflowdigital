import React, { useState, useRef, useEffect } from "react";
import transition from "../../transition";

const AnatolianBreeze = () => {
  return <div>Test</div>;
};

export default transition(AnatolianBreeze);
