import React, { useState, useRef, useEffect } from "react";
import transition from "../../transition";

const AcornAccounting = () => {
  return (
    <div className="flex min-h-screen w-full flex-col items-center justify-center bg-secondary p-4">
      <div className="flex items-center justify-center font-josefin text-2xl text-light">
        Web Application is in Progress...
      </div>
    </div>
  );
};

export default transition(AcornAccounting);
